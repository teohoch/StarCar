<%= simple_form_for(@acquisition) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :employee %>
    <%= f.input :amount_paid %>
    <%= f.association :car_provider %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
